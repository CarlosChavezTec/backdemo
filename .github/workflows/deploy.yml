name: Deploy to Railway

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Instalar Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Instalar dependencias
        run: npm install

      - name: Build (opcional)
        run: npm run build || echo "No build necesario"

      - name: Instalar Railway CLI
        run: npm install -g @railway/cli

      - name: Link al proyecto manualmente
        run: railway link --project 77bcb8f9-946c-4583-bf02-597944f296c1 --environment 29aa1634-32e5-4db1-9326-558f3a699ecf

      - name: Desplegar a Railway
        run: railway up
        env:
          RAILWAY_API_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
